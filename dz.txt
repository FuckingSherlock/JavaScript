a.
    Выведет что неопределено. (! - если 'null' или 'undefined' вернуть false => a не создается)

b. 
    Ошибку, т.к.: 
        1. функция 'a' не находится в глобальной области и вызывается только как метод объекта 'b'
        2. a - имя функции без '()' и аргумента.
c. 
    Код функции, из-за конфликта имен.

d. 
    10

e. 
    Объект текущей области видимости.